import styled from "styled-components";
import Header from "../Components/Header";

const Container = styled.div``;

const Home = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Home;
